package handler

import (
	"net/http"
	"github.com/gin-gonic/gin"
)

// Handler struct holds required functionality of the handler package
type Handler struct {
}

// Config struct will hold required functionality to initialize the handler
type Config struct {

}

// HandlerFactory will initialize the handler
func HandlerFactory (c *Config) {
	
}

